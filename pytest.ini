# pytest.ini
[pytest]
testpaths =
    tests/unit
    tests/integration
    tests/e2e

addopts = -q -ra
asyncio_mode = auto
pythonpath = src
markers =
    unit: unit tests sin IO real
    integration: integra componentes o usa IO real (redis/http)
    e2e: extremo a extremo
    smoke: chequeos rápidos
